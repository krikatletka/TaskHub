services:
  taskhub-api:
    build:
      context: ./TaskHub.Api
      dockerfile: Dockerfile
    ports:
      - "7102:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    volumes:
      - taskhub_data:/app/data
    restart: unless-stopped

volumes:
  taskhub_data:
